import React from 'react';

type Property = {
    [key: string]: any,
};

export default (props: Property) => {
    console.log(props);
    return (
        <div>
            TODO:
        </div>
    )
}
